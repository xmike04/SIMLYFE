Run npm run test:coverage and analyze the report. 
List the 5 files with lowest coverage in src/engine/ and src/config/.
For each, suggest the single highest-value test to add (most risk reduction per test).