# Should NOT trigger

1. "Rename `getUser` to `fetchUser` everywhere." — unambiguous mechanical edit.
2. "What's the difference between Firestore merge and set?" — pure question, no build intent.
3. "Here's SPEC-loans.md, verify the implementation matches it." — spec exists; that's
   frontier-verify's job.
